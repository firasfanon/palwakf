# Error Record

Error:
`permission denied for table core_locations`

Action:
Grant SELECT on `core.core_locations` to `authenticated`, and grant related exact underlying read surfaces likely used by the views/RPCs.
