name: palwakf_shared
description: Shared contracts and UI primitives for PalWakf platform modules.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: '>=3.13.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.0.0

flutter:
  uses-material-design: true
