# Platform Development 10B — Owner-Write RPC Executable Implementation Pack

هذه حزمة تنفيذية موحدة لمسار Owner-Write RPCs بعد إغلاق 10A.

## ترتيب التشغيل

1. `01_owner_write_rpc_executable_implementation_apply.sql`
2. `02_owner_write_rpc_post_apply_catalog_uat_read_only.sql`
3. `03_negative_uat_execution_matrix_read_only.sql`
4. `04_production_gate_redecision_after_10b_read_only.sql`

## حدود أمان ثابتة

- لا ترحيل أو تعديل لـ `auth.users`.
- لا تعديل على `waqf_assets` أو `waqf` أو `awqaf_system`.
- لا `service_role` داخل Flutter.
- Flutter write reroute خلف:

```powershell
--dart-define=PWF_OWNER_WRITE_RPC_WRITE_REROUTE=true
```

القيمة الافتراضية false.

## قرار الإنتاج

10B لا يعتمد الإنتاج. الإنتاج يبقى مغلقًا حتى تُرفق نتائج SQL 02 وNegative UAT وBrowser Console evidence وrollback evidence.


## Platform Development 10C — SQL Apply Result Intake + Anonymous Execute Grant Hotfix

User evidence after running the 10B pack shows:

- SQL 01 apply returned `Success. No rows returned`.
- SQL 02 catalog UAT found all eight owner-write RPCs installed.
- SQL 02 confirmed locked `search_path` and `authenticated` execute grants.
- SQL 02 also detected `anon` execute privilege on all eight RPCs; this is a security blocker.

### 10C execution order

1. Run `05_revoke_anon_execute_owner_write_rpc_hotfix_apply.sql`.
2. Run `06_post_anon_revoke_catalog_uat_read_only.sql`.
3. Run `07_production_gate_redecision_after_anon_revoke_hotfix_read_only.sql`.
4. Run Flutter staging with `--dart-define=PWF_OWNER_WRITE_RPC_WRITE_REROUTE=true` only after SQL 06 shows `anon_blocked=true` for all eight RPCs.

Production remains not approved until Negative UAT and browser console evidence pass.


## Platform Development 10D — anon revoke result intake + public placeholder image console hardening

User evidence after SQL 05/06 shows:

- `anon_blocked=true` for all eight owner-write RPCs.
- `authenticated_execute=true` remains present for all eight RPCs.
- Function presence remains true.
- `owner_write_rpc_anon_execute_revoked_v1=true`.

Browser screenshot evidence shows the app starts in staging and Supabase initializes, but Chrome Console is not clean because an external demo image request fails:

```text
via.placeholder.com/150 net::ERR_CONNECTION_CLOSED
```

10D applies a Flutter runtime hardening patch to avoid known demo placeholder hosts in public image surfaces. Production remains not approved until browser console retest and Negative UAT pass.

Run after applying 10D:

1. `08_platform_development_10d_anon_revoke_browser_console_hardening_uat_read_only.sql`
2. `flutter run -d chrome --dart-define=PWF_OWNER_WRITE_RPC_WRITE_REROUTE=true`
3. Capture Console evidence and Negative UAT actor-case evidence.


## Platform Development 10E

Added:

- `09_platform_development_10e_browser_console_retest_negative_uat_gate_read_only.sql`

Purpose:

- Records public-route browser console retest after 10D placeholder image hardening.
- Preserves SQL06 anon-revoke acceptance.
- Keeps production blocked until Negative UAT actor bundle passes.


## Platform Development 10F — Negative UAT Actor Bundle + Staging Reroute Evidence

Added scripts:

1. `10_platform_development_10f_negative_uat_actor_bundle_matrix_read_only.sql`
2. `11_platform_development_10f_owner_write_rpc_negative_attempt_evidence_template_read_only.sql`
3. `12_platform_development_10f_production_gate_redecision_read_only.sql`

Scope:

- Accept SQL06 anon revoke evidence.
- Accept Flutter staging startup with `PWF_OWNER_WRITE_RPC_WRITE_REROUTE=true`.
- Accept public route console retest for `/home`, `/home/about`, `/home/contact`.
- Keep production blocked until actual Negative UAT actor denial evidence is supplied.

No executable SQL is added in 10F.

---

## 10F Final Result Intake (2026-05-24)

SQL 10–12 outputs confirm accepted prior evidence but do not provide actual denied actor-case evidence. Production remains blocked.

No new executable SQL is added in 10F Final.


## Platform Development 10H — Actual Negative UAT Evidence Bundle

Added read-only scripts:

- `14_platform_development_10h_actual_negative_uat_db_state_probe_read_only.sql`
- `15_platform_development_10h_production_gate_redecision_template_read_only.sql`

Actual denied-attempt evidence is generated by:

```text
tools/platform_development_10h/owner_write_rpc_negative_uat_runner.dart
```

No production approval is granted by SQL 14/15.

## Platform Development 10I — Actual Negative UAT Runner Result Intake

Added read-only scripts:

- `16_platform_development_10i_actual_negative_uat_result_intake_read_only.sql`
- `17_platform_development_10i_sql_rls_no_unsafe_mutation_probe_read_only.sql`
- `18_platform_development_10i_production_gate_redecision_read_only.sql`

Accepted runner evidence:

```text
all_required_actor_cases_denied=true
unsafe_success_count=0
missing_config_count=0
```

Production remains blocked until SQL 17 proof, admin/write-surface browser console clean evidence, and explicit production owner-write reroute approval are supplied.
