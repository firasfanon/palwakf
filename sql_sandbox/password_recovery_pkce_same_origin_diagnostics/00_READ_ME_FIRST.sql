select 'password_recovery_pkce_same_origin_diagnostics_read_only' as section,
       false as ddl_dml_authorized,
       false as grant_revoke_authorized,
       false as production_approved,
       true as read_only;
