-- V4 Wave1 Post-Apply Result Summary (read-only marker)
select
  'v4_wave1_post_apply_result_intake' as section,
  88 as wave1_moved_table_count,
  88 as public_compatibility_view_count,
  88 as target_real_table_count,
  9 as wave3_collision_exclusion_count,
  false as production_approved,
  true as read_only;
