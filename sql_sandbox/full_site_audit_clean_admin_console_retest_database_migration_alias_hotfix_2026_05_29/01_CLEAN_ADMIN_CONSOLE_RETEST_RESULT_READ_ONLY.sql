select
  'full_site_audit_clean_admin_console_retest' as section,
  'auth_token_400_clean_retest' as gate_key,
  true as passed,
  'Clean retest screenshots no longer show repeated POST /auth/v1/token?grant_type=password 400 on the provided admin route set.' as note,
  false as execution_authorized_by_this_script,
  false as production_approved,
  false as destructive_sql_authorized,
  false as exact_public_table_replacement_authorized,
  false as archive_delete_authorized,
  true as no_auth_users_migration,
  true as no_flutter_elevated_secret,
  true as no_waqf_assets_mutation,
  true as no_gis_mutation,
  true as read_only;
