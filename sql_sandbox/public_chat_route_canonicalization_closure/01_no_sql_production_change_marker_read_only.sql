select 'public_chat_route_canonicalization_closure' as section,
       'no_sql_production_change' as check_key,
       true as passed,
       'Route canonicalization closure is Flutter routing only; no database DDL/DML is included.' as note;
