# Platform Instructions Update — N2.29

- Treat database cleanup as a governed development program.
- Always discover table shape before DML.
- Use `source_schema` and `object_name` for `platform.schema_inventory_decisions`.
- Do not move public tables before Movement Gate closure.
- Update governing contract, governance, assistant scope, public chat scope, changelog, and handoff after each major decision.
